import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkForcefullyClose(): void {
  it("Liquidable: forcefullyClose", async function () {});
}
