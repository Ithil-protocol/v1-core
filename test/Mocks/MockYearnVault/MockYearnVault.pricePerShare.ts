import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkPricePerShare(): void {
  it("MockYearnVault: pricePerShare", async function () {});
}
