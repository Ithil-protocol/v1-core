import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkLatestVault(): void {
  it("MockYearnRegistry: latestVault", async function () {});
}
