import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkSetSharePrice(): void {
  it("MockYearnRegistry: setSharePrice", async function () {});
}
