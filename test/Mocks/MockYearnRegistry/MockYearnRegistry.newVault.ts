import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkNewVault(): void {
  it("MockYearnRegistry: newVault", async function () {});
}
