import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

export function checkTotalAllowance(): void {
  it("UniversalStrategy: totalAllowance", async function () {});
}
